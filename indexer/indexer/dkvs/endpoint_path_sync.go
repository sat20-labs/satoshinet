package dkvs

// Endpoint-local FREE_LOCAL data is exposed through the same effective
// snapshot/change-log API as relayable data. There is deliberately no separate
// endpoint overlay object in the wallet-facing protocol.
